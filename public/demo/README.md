# Demo video asset

Place the production demo clip at `public/demo/find-accommodation-demo.mp4` before deployment.

The landing page references that path from `index.html`, but the binary video is intentionally not committed because the PR workflow does not support binary file diffs. Keep the clip short and focused on:

1. Selecting a campus.
2. Browsing accommodation.
3. Viewing listing details.
4. Joining the waitlist.
